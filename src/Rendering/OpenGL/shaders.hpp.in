#pragma once

namespace shader {

const char *essposition_v = R"(@essposition_v.glsl@)";

const char *essposition_f = R"(@essposition_f.glsl@)";

const char *essraycast_v = R"(@essraycast_v.glsl@)";

const char *essraycast_f = R"(@essraycast_f.glsl@)";

const char *position_v = R"(@position_v.glsl@)";

const char *position_f = R"(@position_f.glsl@)";

const char *raycast_v = R"(@raycast_v.glsl@)";

const char *raycast_f = R"(@raycast_f.glsl@)";

const char *screen_v = R"(@screen_v.glsl@)";

const char *screen_f = R"(@screen_f.glsl@)";

} // namespace shader